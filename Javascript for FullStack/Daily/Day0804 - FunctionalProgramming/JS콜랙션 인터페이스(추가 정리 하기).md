# fill

```
Array(5).fill(1); // 크기 5인 배열을 1로 채운다.[1,1,1,1,1]
Array(5).fill(1,2); // 크기 5인 배열을 index=2부터 1로 채운다. [,,1,1,1]
```

# map

```
배열.map((요소, 인덱스, 배열) => { return 요소 });

Array(5).fill().map((x,i)=>i+1); => [1,2,3,4,5]
```

