# 프로그래머스 - 지형이동

- 카테고리 : 시뮬레이션/BFS



# 성공

```
#include <string>
#include <vector>
#include <iostream>
using namespace std;
int answer = 0;
struct INFO{
  int y,x,cnt,dir;
};
queue<INFO> q;
vector<vector<Int>> b;
bool visit[101][101][2];
const int dy[]={0,1,0,-1},dx[]={1,0,-1,0};

void bfs(){
    q.push({0,1,0,0});
    while(!q.empty()){
        auto now=q.front(); q.pop();
        int y=now.y,x=now.x,cnt=now.cnt,dir=now.dir;
        if((y==N && x==N-1) || (y==N-1 && x==N)){
            answer=cnt; return;
        }
        for(int i=0;i<4;i++){
            int ny=y+dy[i],nx=x+dx[i];
            if(ny<1 || nx<1) continue;
            if(i%2==0 && ny>N-1 || nx>N) continue;
            if(i%2!=0 && ny>N || nx>N-1) continue;
            if(visit[ny][nx][dir]) continue;
            visit[ny][nx][dir]=1;
            q.push({ny,nx,cnt+1,dir});
        }
        if(dir==0){
            
        }else{
            
        }
    }
}

int solution(vector<vector<int>> board) {
    b=board;
    return answer;
}
```

